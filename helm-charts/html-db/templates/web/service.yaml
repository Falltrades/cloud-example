apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web-service
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.service.webPort }}
